package cn.sirbox.controller;

import java.util.ArrayList;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.RequestMapping;

import cn.sirbox.service.impl.EnterpriseService;

@Controller
@RequestMapping("statistics")
public class StatisticsController {
    @Autowired
    private EnterpriseService enterpriseService;
    
    
    @RequestMapping("user")
    public String user(Model model){
        Integer year=2016;
        
        List<List<Integer>> list=new ArrayList<>();
        list=enterpriseService.getCountInsert(year);
        System.out.println(list);
        Integer month=new GregorianCalendar().get(new GregorianCalendar().MONTH+1);
        List<String> list1=new ArrayList<>();
        list1.add("注册");
        list1.add("审核通过");
        list1.add("审核未通过");
        list1.add("找资金");
        list1.add("找项目");
        list1.add("找技术");
        
        StringBuffer s=new StringBuffer();
        s.append("[");
        s.append("{");
        s.append("name:'注册',data:[");
        for(int i=0;i<9;i++){
            s.append(i);
            s.append(",");
        }
        s.deleteCharAt(s.length()-1);
        s.append("]}]");
        System.out.println(s);
        model.addAttribute("list1", list1);
        model.addAttribute("s", s);
        return "statisticsuser";
    }
}
